{{> layout/header}}
    <div class="container p-5">
        <div class="card">
            <h2>관리자 대시보드</h2>
        </div>
        <div class="card-body">
            <p class="mb-1">안녕하세요, <b>{{user.username}}</b></p>
            <p class="text-muted mb-0">이 페이지는 관리자만 접근 가능합니다.</p>
        </div>

    </div>

{{> layout/footer}}